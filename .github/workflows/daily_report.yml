name: Core Portfolio Daily Report

on:
  schedule:
    # 21:30 UTC = 06:30 KST (매일)
    - cron: '30 21 * * *'
  workflow_dispatch: {}   # Actions 탭에서 수동 실행(테스트)용

jobs:
  send-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compute core signals & send Telegram message
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_TO: ${{ secrets.TELEGRAM_TO }}
        run: python core_signals.py
